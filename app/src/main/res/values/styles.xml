<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionBarStyle">@style/QuizAppBarStyle</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- App bar style -->
    <style name="QuizAppBarStyle" parent="style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <!-- Remove the shadow below the app bar -->
        <item name="elevation">0dp</item>
    </style>


    <!-- Style for a tab that displays a category name -->
    <style name="CategoryTab" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/category_civilAccent</item>
        <item name="tabSelectedTextColor">@color/category_mechanicalAccent</item>
        <item name="tabTextAppearance">@style/CategoryTabTextAppearance</item>
    </style>

    <!-- Text appearance style for a category tab -->
    <style name="CategoryTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">#FFFF</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!--Style for the App-->
    <style name="CategoryStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:padding">16dp</item>
        <item name="android:layout_marginBottom">4dp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
    </style>

    <!-- Electrical Engineering theme. -->
    <style name="ElectTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/category_electrical</item>
        <item name="colorPrimaryDark">@color/category_electricalDark</item>
        <item name="colorAccent">@color/category_electricalAccent</item>
    </style>

    <!-- Civil Engineering theme. -->
    <style name="CivilTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/category_civil</item>
        <item name="colorPrimaryDark">@color/category_civilDark</item>
        <item name="colorAccent">@color/category_civilAccent</item>
    </style>

    <!-- Mechanical Engineering theme. -->
    <style name="MechTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/category_mechanical</item>
        <item name="colorPrimaryDark">@color/category_mechanicalDark</item>
        <item name="colorAccent">@color/category_mechanicalAccent</item>
    </style>


</resources>
